import PieGraphConfig from './configType';

const pieGraphDefaultConfig: PieGraphConfig = {
  canvasWidth: 512,
  canvasHeight: 512,
  animationStartPoint: 0,
  animationEndPoint: 100,
  animationCoefficient: 32,
  pieRadius: 128,
  pieLineWidth: 64,
};

export default pieGraphDefaultConfig;
